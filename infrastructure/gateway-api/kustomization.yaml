apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Standard Gateway API v1.6.1, supported by Cilium 1.20.2.
  # Keep this pin aligned with elektrokube-scripts/config/cluster.json.
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.1/standard-install.yaml
